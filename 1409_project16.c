#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int is_max_number(int a, int b);
int a, b;

int main(void)
{
	int num1, num2;
	
	 
	printf("첫번째 정수를 입력하세요 : ");
	scanf_s("%d", &num1);

	printf("두번째 정수를 입력하세요 : ");
	scanf_s("%d", &num2);

	is_max_number(a,b);
	
	return 0;
}

int is_max_number(int a, int b)
{
	
}