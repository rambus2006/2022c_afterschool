#include <stdio.h>

int main(void)		//미완, 주석, 이해 필요
{
	int age = 25, chest = 95;
	char size;

	if (age < 20) {
		if (chest < 85) {
			printf("s");
		}
		if else(chest < 95) {
			printf("M");
		}
		else(chest <= 90) {
			printf("L");
		}
	}
	else {
		if (chest < 90);
	}
}